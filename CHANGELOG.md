**日本語** | [English](CHANGELOG.en.md)

# 更新履歴

[README に戻る](README.md)

### 2026.09.06.

- **画面下部に「多機能操作バー」を追加しました**（既定でオン。**Windows / macOS 両方**）。1 行に左から、ページ位置の**スライダー**、**ページ番号**（「12 / 196」。2 ページ表示では「12-13 / 196」）、**最初のページへ / 前のページへ / 次のページへ / 最後のページへ**の 4 つのボタンを並べています。スライダーはクリックやドラッグでそのページへ移動し、ボタンはキー割り当てと同じ動きをします（本の端では従来どおり効果音・明滅・フロート表示で知らせ、「本の端での動作」の設定にも従います）。デザインはマテリアルデザイン風のシンプルなもので、アイコンとつまみにはアンチエイリアスが掛かっています。
  - **全画面表示では普段は隠れていて、カーソルを画面の下端へ持っていったときだけ画像の上に重なって出ます**（タイトルバーと同じ振る舞い）。通常表示ではバーの高さ（40）のぶん画像の領域が縮みます。
  - これに合わせて、ページ移動バーの出し方を**「表示しない / 多機能操作バー / ページスライダー」の 3 択**にしました。〔表示〕メニューの**「ページ移動バー」**か、設定アプリ［表示］タブの同名の項目で選べます。「ページスライダー」は従来どおりタイトルバーの下端に細い線で出す方式（**Windows 版のみ**）、「表示しない」で両方消えます。これまで「ページスライダーを表示する」をオフにしていた場合は「表示しない」として引き継がれます。
  - macOS 版はこれまでページ位置の表示と移動の手段が無かったので、この多機能操作バーがその役を担います。〔表示〕メニューの「ページ移動バー」は「表示しない / 多機能操作バー」の 2 択です。
  - コマンドは 3 個増えて **176 個**になりました（「ページ移動バー: 表示しない / 多機能操作バー / ページスライダー」）。従来の「ページスライダーの表示切り替え」は**「ページ移動バーの表示切り替え」**になり、非表示にする直前に使っていた方式との間で入れ替わります。
  - **速度には影響しません。**バーの部品は起動時に裏で作り、ページ送りではつまみとページ番号の帯だけを描き直します。実測（ページ送り 1 回のキャッシュ済みの中央値）: Windows 6.0ms 前後、macOS 29ms 前後で、バー無し / 多機能操作バー / ページスライダーの 3 設定に差はありません。

### 2026.09.05.

- **画像フォルダでも「次の書籍に移動」「前の書籍に移動」が使えるようになりました。**移動先は同じ階層にある隣のフォルダで、並びは書庫と同じ自然順（「1話 / 2話 / … / 10話」の順）です。書庫を開いているときは従来どおり書庫ファイルの間だけを移動し、フォルダと書庫を混ぜて数えることはしません。隠しフォルダは飛ばします。
  - **画像の無いフォルダへ移動したときは、そのフォルダを開いた状態になります。**タイトルバーに「フォルダのフルパス (画像がありません)」と出て絵は消えますが、そこを現在地としてさらに次 / 前のフォルダへ進めます。これまでは前の本に居続けたため、途中に空のフォルダがあると先へ進めませんでした。空のフォルダをドロップしたときや、ファイルを開くダイアログで選んだときも同じ状態になります（空の書庫は従来どおり前の本を出したまま知らせるだけです）。

- **直下に画像が無いフォルダを開くと「画像がありません」になる問題を修正しました。**設定アプリ［表示］タブの「サブフォルダを読み込む」がオンでも、作品フォルダ（直下に画像は無く、各話のフォルダが並んでいる）を開くとサブフォルダを読まずに諦めていました。起動と同時に開くときだけ設定を読む前に列挙していたためで、諦める前に設定を待ってサブフォルダ込みで列挙し直すようにしました。

- **設定アプリは常に 1 つだけ起動するようになりました**（**Windows 版のみ**）。2 つ目を起動すると、開いている設定アプリのウインドウを前に出して終わります。本体が「常に手前に表示」のときの「常に手前」の指定や、開くタブの指定もそのウインドウへ渡します。2 つの画面から同じ設定ファイルを別々に編集して、後に保存した側が先の変更を上書きしてしまうことを防ぐためです。

- **重いページの先読みを見直しました。**1 ページのデコードに時間がかかる本（3000×5000 級の可逆圧縮など）で、次のページが揃うまでの待ちが短くなります。
  - これまで先読みは「1 スレッド × ワーカ数」で並んだページを同時に解いていました。軽いページでは最速ですが、1 ページに 1 秒以上かかる本では**隣のページが 8 ページ先と同じ時刻にしか出来上がらず**、めくった瞬間に「その場でデコード」の待ちが入っていました。1 ページの重さをファイルの大きさから見積もり、重いと分かったページだけ**「ワーカ数の半分のスレッド × 2 基」**で隣から順に解くようにしました。CPU の使い方（スレッドの総数）は変わりません。
  - 実測（大判の可逆 jxl、2 ページ表示、0.5〜1 秒おきにめくる）: 隣の見開きが揃うまで **1.9 秒 → 0.7 秒**（Apple M4）。めくったときに先読みが間に合わなかった場合の待ちも、途中まで解けているページを待つようにして **215ms → 130ms**（Ryzen 9 9900X）/ **387ms → 161ms**（M4）。先読みが間に合っているときの速さと、軽い本の速さは変わりません。
  - 設定アプリ［性能］タブに**「重いページのしきい値 (ms)」**を追加しました（既定 200）。1 スレッドでこの時間を超えると見積もったページを「重い」とみなします。**0 にすると従来どおり**の動きになります。
  - 情報パネルの【システム】に**「重い段」**の行を追加し、いま使っているデコーダの構成と、重さの見積もりの元になっている実測値を出します。
  - 情報パネルの【画像】に**「デコード」**の行を追加しました。表示中のページのデコードにかかった時間と、誰が解いたか（先読み / 先読み・重い段 / 表示要求 = 先読みが間に合わずその場で解いた）、使ったスレッド数を出します。重い本で「表示要求」が続くなら、先読みの設定を見直す手掛かりになります。時間には書庫からの取り出しも含みます。
  - 効くのは jxl と avif です（webp と OS 標準のデコーダはページ内の並列処理ができないため）。
- 診断用の `--log <パス>` を、`--bench` / `--shot` だけでなく通常起動と `--selftest` でも受けるようにしました。

### 2026.09.03.

- **フロート表示を追加しました。**画像の上に半透明の帯で短い文字列を重ねて出し、しばらくすると消えます。出すのは次の 3 種類で、設定アプリに追加した**［フロート表示］タブ**で全体の入切と種類ごとの入切ができます（既定はすべてオン）。
  - **開いたファイル名** — 本を開いたとき（起動時・ドロップ・前後の本への移動など）に出します。出す形は〔ファイル〕メニューの「ファイル名をクリップボードへ」と同じ「ファイル名 / ファイル名フルパス / フォルダ名 / フォルダ名フルパス」の 4 つから、**書庫**と**画像ファイル・フォルダ**で別に選べます（既定は書庫が「ファイル名」、画像ファイル・フォルダが「フォルダ名フルパス」。画像ファイル・フォルダの「ファイル名」は開いた時点で表示している画像です）。
  - **警告** — 最後のページで次へ進もうとしたときの「最後のページです」、隣の本が無いときの「次の本がありません」など、移動が端で止まったとき。読み込み失敗や削除できなかったときなど、これまでタイトルバーにだけ出していたエラーも同じ文言で出します。
  - **マウスジェスチャーの進行** — 右ボタンを押したまま方向を描いている間、描いた矢印と「いま離すと実行されるコマンド」を「→  前のページへ」のように出し続けます。手を足すたびに更新され、割り当てが無い列は「(割り当てなし)」と出ます。離して実行されると末尾に **「[実行]」** が付いて表示時間だけ残り、実行されなければ消えます。
  - ［フロート表示］タブでは、表示位置（画像領域の左上からの割合、X / Y 各 0〜100%、既定 2%）、フォントと大きさ（既定はシステムの既定フォント・14pt）、文字色（既定 白）、背景色と不透明度（既定 黒・75%）、表示秒数（0.5〜10 秒、既定 2 秒）、**消えるまでの時間**（表示秒数を過ぎてから文字と背景が一緒に薄くなって消えるまでの長さ。0〜2 秒、既定 0.3 秒。0 なら即座に消えます）を指定できます。
  - **速度には影響しません。**出していない間はページ送りに何も載らず、フォントは最初に表示するときに裏で読み込みます（そのため起動直後の 1 回目は少し遅れて出ることがあります）。

- **設定アプリのウインドウが、高さの低いモニタで画面からはみ出す問題を修正しました**（**Windows 版のみ**）。
  - 開いたときにモニタの作業領域（タスクバーを除いた範囲）へ収まるようウインドウを縮めます。これまでは既定の高さが 1366×768 級のモニタより高く、下端の［OK］［キャンセル］［適用］などが画面外に切れることがありました。前回のサイズを復元して開くときも同様に収めます。
  - ［操作］タブで、ウインドウが低いと「キー割り当て」枠の右側にある「既定に戻す」ボタンが切れて押せなくなっていました。各欄が潰れない高さを確保し、それでも収まらないときはタブ全体にスクロールバーを出すようにしました。

### 2026.08.31.

- **スクロール操作を追加しました。**画像がウインドウからはみ出しているとき、キーやホイールで表示位置を動かせます。コマンドは 6 つで、設定アプリ［操作］タブで割り当てます。
  - **「上へ / 下へ / 左へ / 右へスクロール」** — はみ出している向きへ一定量動きます。端では止まります（ページ移動はしません）。
  - **「下へスクロール / 次のページへ」「上へスクロール / 前のページへ」** — スクロールしきったらページを送ります。送った先は読みの流れが繋がるよう、**進むときは上端・戻るときは下端**から表示します。ホイールに割り当てると「縦に収まらないページはそのページの中でスクロールし、下端まで見たら次のページへ進む」という読み方ができます。
  - 1 回の移動量は設定アプリ［表示］タブに追加した**「スクロール量」**（ウインドウの辺に対する割合、5〜100%、既定 25%）で調整できます。
- 設定に**「ページ移動のたびに表示位置を読み始め側（進むときは上端）に揃える」**を追加しました（既定はオフ）。「幅に合わせる」のようにページがはみ出す表示で、これまでページを送ると中央が表示されていましたが、オンにすると必ず読み始め側（進むときは上端＋開き方向の始端、戻るときは下端）から表示します。収まっているページでは従来と変わりません。
- **スクロールバー表示**を追加しました（既定はオフ）。〔表示〕メニューの「スクロールバー」か設定アプリ［表示］タブでオンにすると、画像がはみ出している軸にだけ、ウインドウの右端・下端に表示位置の目印（つまみ）を重ねます。パン・スクロール・ズームの現在位置が一目で分かります。
- **段階的ズーム（「ズームイン」「ズームアウト」「ズーム解除」）**を追加しました。カーソル直下の点を留めたまま、**「1段の倍率」**（設定アプリ［表示］タブ、1.1〜4.0、既定 1.25 倍）ずつ拡大・縮小します。「ズーム解除」と従来の「ズーム機能ON/OFF」の切りは、ズーム前の表示位置ごと元に戻します。〔表示〕メニューに**〔ズーム〕サブメニュー**（ON/OFF / イン / アウト / 解除）も追加しています。
- 上記もすべて**キーやマウスに割り当てられます**（設定アプリ［操作］タブ）。コマンドは 10 個増えて **173 個**になっています。
- **既定の設定・割り当てのままでは、動作はこれまでと変わりません。**

### 2026.08.18.

- マウスジェスチャー（右クリック＋方向）を**「上下左右の組み合わせを最大 3 手まで」**に拡張しました（`↑` `↑↓` `←↓→` など 84 通り）。右ボタンを押したまま方向を続けて描き、**ボタンを離した時点で**割り当てたコマンドが実行されます。
  - 設定アプリ［操作］タブに**「マウスジェスチャー」欄**を新設しました。方向を 3 つまで選んで「追加」で列を作り、列ごとにコマンドの割り当てと削除ができます。**従来の 4 方向（1 手）の割り当てはそのまま引き継がれます。**
  - 4 手以上を描いた場合は誤操作と見なし、何も実行しません（右クリックにも落ちません）。
  - `suzunia.json` を直接編集する場合の綴りは、`RightGesture` に方向（`Up` / `Down` / `Left` / `Right`）を描く順で連結したものです（例: `RightGestureUpDown`）。
  - これに伴い、1 手のジェスチャーの発動タイミングが**「方向を描いた瞬間」から「ボタンを離した瞬間」**に変わっています（「↑」と「↑↓」のような組み合わせを共存させるため）。
- 〔エフェクト〕メニューを**「プリセット」と「即席カスタマイズ」の 2 段**にしました。どちらの段かはグレーの見出しで示しています。下段では、**プリセットの内容を書き換えずにその場だけ**各エフェクトを差し替えられます。並びは 補間方法 / リニア光で補間 / アンシャープマスク / 自動レベル補正 / 疑似カラー / アップスケーリング（アップスケーリングは **Windows 版のみ**）。
  - **補間方法** — 7 種類から選べます（並びは「補間法を順に切り替え」と同じ、使用頻度の高い順）。
  - **疑似カラー** — 「なし」＋「プリセットの配色」＋**組み込みパターン 32 種**。設定アプリ［エフェクト］タブの「パターン」と同じ品揃えで、選ぶと配色（停止点）だけがそのパターンになります（強さはプリセットの値のまま）。チェックは停止点の値で決まるので、設定アプリで停止点を手編集したプリセットでは「プリセットの配色」に付きます。
  - **アップスケーリング** — 掛けない / 線画復元 / 線画復元 CNN。強さと「何倍から掛けるか」は設定アプリの持ち物なので、強さ 0 のプリセットでは方式を選んでも掛かりません。
  - 即席カスタマイズ中は、メニューと情報パネルのプリセット名に**「（変更あり）」**と出ます。**プリセットを選び直す**（同じ番号でもよい）か、**設定を再読み込み**するか、メニュー末尾の**「即席カスタマイズを破棄」**で元へ戻ります。**本を替えても消えません。**残したい組み合わせは設定アプリでプリセットにしてください。
- 上記も**キーやマウスに割り当てられます**（設定アプリ［操作］タブ）。コマンドは 12 個増えて 163 個になっています。**疑似カラーの 32 パターンだけはメニュー専用**です（一覧が読めなくなるため。入切は従来の「疑似カラー化の入切」で割り当てられます）。
- ページスライダーに**「クリック判定範囲の表示」**を追加しました（**Windows 版のみ**、既定オン）。クリック / ドラッグでページ移動できる範囲（タイトルバー下端のスライダーの帯と、その下の画像上部の当たり判定）にカーソルが入ると、判定範囲全体をスライダーと同じ塗り分けの半透明で表示します。「いまクリックするとページ移動になる」ことが一目で分かり、画像上部のクリック操作との取り違えを防げます。設定アプリ［動作］タブの**「カーソルを合わせたときクリック判定の範囲を表示する」**で無効にもできます。
- ページスライダーを**ドラッグしている間、カーソルが判定範囲の中に閉じ込められる**ようになりました。押したまま左右へ動かすとその位置のページへ追従します（従来どおり）。ボタンを離すか、Alt+Tab などでウインドウから離れると解除されます。

### 2026.08.16.

- 〔ファイル〕メニューに**「ファイル名をクリップボードへ」**を追加しました。「現在のファイル名」「現在のファイル名フルパス」「現在のフォルダ名」「現在のフォルダ名フルパス」の 4 つを、文字列としてクリップボードへコピーできます。ここでいう「現在のファイル」は「エクスプローラで開く」と同じもので、書庫・PDF・EPUB では書庫ファイル自体、画像ファイルや画像フォルダを開いているときは表示中の画像ファイルです。
- 〔ファイル〕メニューに**「インストール先フォルダを開く」「設定ファイル保存先フォルダを開く」**を追加しました。設定ファイルの場所は、Windows では通常 exe と同じフォルダですが、書き込めない場所 (Program Files 直下など) に置いた場合は `%APPDATA%\suzunia`、macOS では `~/Library/Application Support/suzunia` になります。
- 〔ファイル〕メニューに**「他のアプリで開く」**を追加しました (**Windows 版のみ**)。表示中のファイルを画像編集ソフトなどに渡せます。設定アプリに追加した**［他アプリ］タブ**で 9 個まで登録でき、1 つにつき「メニュー名」「実行ファイルのフルパス」「引数」「起動後に suzunia をどうするか」を指定します。
  - **引数** — 「現在のファイルパス」(既定) / 「現在のフォルダパス」/ 「画像をクリップボードへコピー」/ 「何も送らない」。「画像をクリップボードへコピー」は、表示中のページを原寸でクリップボードへ置いてから引数無しで起動します (貼り付けは起動したアプリ側で行ってください)。
  - **起動後** — 「何もしない」(既定) / 「最小化」/ 「最大化」/ 「終了」。
  - メニューに出るのは**メニュー名と実行ファイルの両方を入れた項目だけ**です。実行ファイルには .exe のほか .bat やショートカット (.lnk) も指定できます。
- 上記をすべて**キーやマウスにも割り当てられる**ようにしました (設定アプリ［操作］タブ)。コマンドは 15 個増えて 151 個になっています。

- 先読みロードのインジケータ「＊」マークの表示にバグがあったので修正しました。

### 2026.08.15.

- **ページ送りを高速化しました（先読みプリレンダ）。**次に表示する見開きを、表示サイズへの縮小からアンシャープマスクまで裏で準備しておき、ページ送りを画面への転送だけにします。大きいウインドウや大判のページほど効きます（実測: 4K・2ページ表示で切り替え 5.9ms → 2.4ms）。絵は従来と完全に同じです。設定アプリ［性能］タブの「次のページの表示用画像を先読みで準備する」（既定オン）で無効にもできます。
- 設定に**「ページ送りの連打中は描画品質を落として速度を優先する」**を追加しました（既定はオフ）。キーを押しっぱなしにするなど、指定した間隔（スライダー: 0.05〜0.5 秒、既定 0.2 秒）未満でページ送りが続いている間だけ補間を軽くしてアンシャープマスクを省き、手が止まるといつもの品質で描き直します。
- ページ送りのたびに走っていたタイトルバーの再描画を間引き、1 めくりあたりのコストをわずかに下げました（表示は最大 0.05 秒遅れて追いつきます）。
- ページ移動に**「割合ページ進む / 戻る」**を追加しました。総ページ数に対する割合で移動するので、本の長さに関わらず同じ「読み進み具合」で飛べます。割合は設定アプリ［表示］タブの「ページ移動量」に**スライダー（1〜100%、既定 5%）**で追加しました。〔移動〕メニューからも実行できます（キーの割り当ては［操作］タブ）。
- 設定に**「ゴミ箱に入れるときは確認ダイアログを出す」**を追加しました（既定はオフ）。これまで確認が出るのは「直接削除」のときだけでした。
- **画像ファイル・フォルダでも2ページ表示にできるようにしました。**これまで画像ファイルやフォルダを直接開いたときは1ページ表示に固定でしたが、〔ページ〕メニューやキーの割り当てから**2ページ表示へ切り替えられる**ようになりました。設定アプリ［動作］タブに追加した**「画像ファイル・フォルダでも2ページ表示にする」**（既定はオフ）をオンにすると、開いた直後から［表示］タブの「ページ表示」に従って開きます。書庫や PDF には影響しません。
- 設定に**「ドロップしたとき suzunia をアクティブにする」**を追加しました（既定はオン）。ウインドウにファイルを落としたとき、suzunia を前面に出します。オフにすると本は開きますが、ドロップ元（エクスプローラーなど）が前面のままになります。ファイルの関連付けから開いたときや、多重起動を許可していないときの受け渡しは、この設定に関わらず前面に出ます。
- **全画面表示でもウインドウボタンを出す**ようにしました。画面の上端にカーソルを合わせてタイトルバーを出したとき、これまでは最小化・閉じるボタンがありませんでした。真ん中のボタンは**全画面の解除**として働きます。
- ページスライダーを**一段太く、明るく**しました。
- **「Alt+1」のような「Alt+英数字」の割り当てでエラー音が鳴る**不具合を修正しました。割り当てたコマンド自体は動いていましたが、そのあと Windows が「メニューに無い組み合わせ」と判定して音を鳴らしていました。あわせて、Alt+F のように**メニュー見出しの頭文字へ割り当てるとメニューまで開いてしまう**問題も直しています（この場合、そのメニューは Alt では開かなくなります。クリックでは開きます）。

### 2026.08.14.

- **Mac版 (Apple Silicon) を公開しました。**

- 設定画面を開いたとき、［エフェクト］タブの「編集するプリセット」が**ビューア本体でいま使っているプリセット**から始まるようにしました。
- ［エフェクト］タブの「白黒 / カラーの判定」を**プリセットごとではなく全体で共通**にしました。「設定A / 設定B」と一緒にタブの一番下の**「共有設定」**へ移動しています。
- 疑似カラーのパターンに**「トーン着色」**（5 色）を追加しました。純黒と純白はそのままで、中間調（トーン）だけが肌色に寄ります。
- エフェクトプリセット **9 番の既定を「トーン着色（疑似カラー）」**に、**「エフェクト: AとBを切り替える」の B の既定を 9 番**にしました（既定のままの場合。設定済みの内容は変わりません）。

### 2026.08.13.

- **英語/日本語の切り替え** に対応しました。
- **TGA** フォーマット、 **PSD** フォーマットに対応しました。
- 「チャンネルピッカー」機能を追加しました。RGBAいずれかのチャンネルのみを表示できます。
- 疑似カラーのパターン/エフェクトのプリセットを整備しました。
- 疑似カラー化/自動レベル補正/背景色に関連するバグをいくつか修正しました。
- アップスケーリングのパラメータ調整を行いました。

### 2026.08.12.

- エフェクト「自動レベル調整」を追加しました。
- エフェクト「疑似カラー化」を追加しました。
- 「2ページ表示時にサイズ調整」機能を追加しました。

### 2026.08.11.

- アップスケーリング機能を追加しました。軽量な代わりに低品質です。
- ICCプロファイルに対応しました。
- エフェクト周りをまとめ、プリセットとして管理出来るようにしました。

### 2026.08.10.

- 「余白の自動カット」機能を追加しました。
- 「横長ページの分割表示」機能を追加しました。
- 90度ずつの回転に対応しました。
- ランダムなページ/書籍への移動を追加しました。
- 圧縮ファイルの削除に対応しました。
- タイトルバー描画を微調整しました。
- 関連付け設定の不具合を修正しました。

### 2026.08.09.

- epubフォーマットに対応しました。
- 設定アプリ上にシェル統合(右クリックメニュー、関連付け)タブを追加しました。

### 2026.08.08.

- しおり（ブックマーク）機能を追加しました。
- 自動ページ送り（スライドショー）機能を追加しました。2パターンの速度を指定できます。
- マウスジェスチャー操作に対応しました。
- 書庫内書庫に対応しました。
- 先読み状況を視覚化しました。タイトルバー左上のページ数に"＊"が付いている間は先読みロードが走っています。
- ファイルの削除（ゴミ箱行き/直接削除）機能を追加しました。
- ファイルの再読み込み機能を追加しました。
